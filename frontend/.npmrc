# Use HTTPS (required by npm since Oct 2021)
registry=https://registry.npmjs.org/
prefer-offline=false
fund=false
audit=false

# Reduce path depth on Windows (npm 7+)
# If you still get TAR_ENTRY_ERROR / ENOENT, move project to a short path (e.g. C:\iasms\frontend) or use pnpm.
